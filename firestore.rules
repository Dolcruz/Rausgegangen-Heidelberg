rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read-only public access to events
    match /events/{eventId} {
      allow read: if true;
      allow write: if false;
    }
  }
}


