{
  "name": "functions",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -w -p tsconfig.json",
    "lint": "echo 'no linter configured in functions'",
    "serve": "node --enable-source-maps lib/index.js",
    "smoke": "node --enable-source-maps lib/test.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1"
  },
  "devDependencies": {
    "typescript": "~5.8.3"
  }
}


